<Window x:Class="CrestiUI.LobbyListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LobbyListWindow" Height="450" Width="800"
        DataContext="{Binding Mode=OneWay, RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" x:Name="LobbyDataGrid" ItemsSource="{Binding LobbyList}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Click="Connect_Click"> присоединиться</Button>
            <Button Click="Create_Click">создать</Button>
        </StackPanel>
    </Grid>
</Window>